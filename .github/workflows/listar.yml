  name:

  on:
    push:
      branches: [main]
    workflow_dispatch:
  jobs:
    listar:
      runs-on: ubuntu-latest
      steps:
        -  name: Checkout code
           uses: actions/checkout@v3

        -  name: Listar
           run:  |
                 cd ../
                 ls
                
